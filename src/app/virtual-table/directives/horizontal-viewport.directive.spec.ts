import { HorizontalViewportDirective } from './horizontal-viewport.directive';

describe('HorizontalViewportDirective', () => {
  it('should create an instance', () => {
    const directive = new HorizontalViewportDirective();
    expect(directive).toBeTruthy();
  });
});
