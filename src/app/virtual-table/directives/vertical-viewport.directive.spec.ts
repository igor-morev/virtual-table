import { VerticalViewportDirective } from './vertical-viewport.directive';

describe('VerticalViewportDirective', () => {
  it('should create an instance', () => {
    const directive = new VerticalViewportDirective();
    expect(directive).toBeTruthy();
  });
});
